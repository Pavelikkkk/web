import React from 'react'; // ядро
import ReactDOM from 'react-dom/client'; // для внедрения компонентов В приложение
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
);

